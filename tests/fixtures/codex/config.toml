# Codex project configuration for MAP Framework

[features]
# Enable hooks for MAP workflow enforcement
hooks = true

[agents]
# Role definitions are auto-discovered from .codex/agents/*.toml.
max_threads = 6
max_depth = 2
